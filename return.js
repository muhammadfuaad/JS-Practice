function myAge(age = 28) {
  return age
}

myAge()

function addNumbers(a, b) {
  return a + b;
}

let result = addNumbers(3, 4);
console.log("result:", result); // Output: 7
console.log("addNumbers(3, 4):", addNumbers(3, 4));

// Notes
// return statement is used to save the final output of the function which can be used later as well.
// console just shows the output for that particular point only.